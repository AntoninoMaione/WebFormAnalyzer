# SurveyJS Preview Module (Phase 2)

This folder will contain the SurveyJS form preview and editor component.

## Planned files
- `preview.html` — renders a SurveyJS JSON schema using Form Library (jQuery)
- `editor.html` — embeds Survey Creator for manual schema refinement
- `export.js` — handles JSON schema export to file or clipboard
