# PDF Analyzer Module (Phase 2)

This folder will contain the PDF upload and field extraction component.

## Planned files
- `index.html` — entry point for the demo app
- `analyzer.js` — PDF parsing and field detection logic
- `mapper.js` — maps detected fields to SurveyJS question types
- `schema-generator.js` — generates the final SurveyJS JSON schema
